#include <bits/stdc++.h>
#include "testlib.h"

using namespace std;

int main(int argc, char *argv[]) {
    registerValidation(argc, argv);
    string s = inf.readString("[0-9]+", "s");
    string t = inf.readString("[0-9]+", "t");
    int n = inf.readInt(1, 1000, "n");
    inf.readEoln();
    for (int i = 0; i < n; i++) {
        string a = inf.readString("[0-9]+", "a");
    }
    inf.readEof();
}