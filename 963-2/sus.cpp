#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define MOD 998244353
#define MOD2 1000000007
#define vt vector
struct pii {int x, y;};
struct pll {ll x, y;};

int T, N;
int a[200005];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int ans = 0;
    for (int i = 1; i <= 6; i++) {
        for (int j = 1; j<= 4; j++) {
            for (int k = 1; k <= 3; k++) {
                int res = i*j*k;
            }
        }
    }
    return 0;
}